# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=7b2a6b6959

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_put_success.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Update a product in the store"
    flow:
      - put:
          url: "/products/{{ id }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'id'
            - hasProperty: 'name'
            - hasProperty: 'description'
            - hasProperty: 'price'
          capture:
            - json: "$.id"
              as: "productId"
            - json: "$.name"
              as: "productName"
            - json: "$.description"
              as: "productDescription"
            - json: "$.price"
              as: "productPrice"
          log: "Product with id {{ productId }} updated successfully with name {{ productName }}, description {{ productDescription }}, and price {{ productPrice }}"
      - think: 1
