# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=7b2a6b6959

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p99: 200
  plugins:
    expect: {}
  payload:
    path: "products_productId_put_success.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
scenarios:
  - name: "Update product scenario"
    flow:
      - log: "Updating the product with a valid payload"
      - put:
          url: "/products/{{ productId }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "id"
            - hasProperty: "description"
            - hasProperty: "price"
        # The product should be updated successfully and return a 200 status code with the updated product details in the response body.
      - think: 1
