# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# Test generated for /products/{productId}_put for http method type PUT in artillery framework
# RoostTestHash=7b2a6b6959

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_put_success.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Update a product"
    flow:
      - log: "Starting scenario to update a product"
      - put:
          url: "/products/{{ id }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
      - log: "Product update request sent"
      - think: 1
      - log: "Ending scenario to update a product"
