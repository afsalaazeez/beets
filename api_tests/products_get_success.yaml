# Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-3.5-turbo-16k

# RoostTestHash=70199667a6

config:
  target: '{{ $processEnvironment.API_HOST }}'
  plugins:
    expect:
      enabled: true
scenarios:
  - name: Test scenario for API endpoint /products
    flow:
      - post:
          url: /products
          headers:
            Authorization: '{{ $processEnvironment.AUTH_TOKEN }}'
          payload:
            skipHeader: true
            csv: products_get_success.csv
          log: Sending request to get all products
        think: 1
      - expect:
          statusCode: 200
          json:
            - $.id:
                is: number
            - $.name:
                is: string
            - $.description:
                is: string
            - $.price:
                is: number
          log: Validating response for getting all products
        think: 1
