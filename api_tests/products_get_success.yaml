# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /rest/products_get for http method type GET in artillery framework

# RoostTestHash=77bd829686


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_get_success.csv"
    fields: 
      - "productId"
      - "productName"
    skipHeader: true
  headers:
    "{{ SECURITY_SCHEMA.name }}" : "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test Products API"
    flow:
      - get:
          url: "/rest/products"
          headers:
            "{{ SECURITY_SCHEMA.name }}" : "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "products"
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
        # log: "Successfully fetched the list of products"
        think: 1

      - get:
          url: "/rest/products/{{ productId }}"
          headers:
            "{{ SECURITY_SCHEMA.name }}" : "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "product"
            - hasProperty: "product.productName"
            - equals: {"product.productName": "{{ productName }}"}
        # log: "Successfully fetched the product details"
        think: 1
