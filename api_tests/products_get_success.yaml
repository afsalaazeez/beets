# Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Get All Products"
    flow:
      - log: "Sending GET request to /products to fetch all products"
      - get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'id'
            - hasProperty: 'name'
            - hasProperty: 'description'
            - hasProperty: 'price'
      - think: 1
