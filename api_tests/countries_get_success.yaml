# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /rest/countries_get for http method type GET in artillery framework

# RoostTestHash=0377fbf6fd


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "countries_get_success.csv"
    fields:
      - "CountryName"
      - "CountryCode"
    skipHeader: true
  plugins:
    expect: {}
    
scenarios:
  - name: "Get countries list"
    flow:
      - get:
          url: "/rest/countries"
          headers:
            Authorization: "{{#if $processEnvironment.AUTH_TOKEN}}Bearer {{ $processEnvironment.AUTH_TOKEN }}{{/if}}"
          expect:
            - statusCode: 200
            - hasProperty: 'CountryName'
            - hasProperty: 'CountryCode'
          log: "Sent a GET request to /rest/countries, expected a 200 status code and properties 'CountryName' and 'CountryCode'"
      - think: 1
