# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /rest/countries_get for http method type GET in artillery framework

# RoostTestHash=0377fbf6fd


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "countries_get_success.csv"
    fields:
      - "countryId"
    skipHeader: true
  headers:
    "Authorization": "Bearer {{$processEnvironment.AUTH_TOKEN}}"
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Get a list of countries"
    flow:
      - get:
          url: "/rest/countries"
          expect:
            - statusCode: 200
            - hasProperty: "countries"
          # Expectation: The API should return a list of countries with status code 200
          headers:
            "Authorization": "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      - think: 5
