# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /products/{productId}_get for http method type GET in artillery framework

# RoostTestHash=ab8efa24ed


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_success.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Test GET /products/{productId} API"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
        # Pause for 1 second between requests
        think: 1
      - log: "Successfully fetched product details"
