# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    - expect: true
  payload:
    path: "products_productId_get_success.csv"
    fields: ["productId"]
    skipHeader: true

scenarios:
  - name: "Find product by ID"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$.id"
              as: "id"
            - json: "$.name"
              as: "name"
            - json: "$.description"
              as: "description"
            - json: "$.price"
              as: "price"
          log: "Sending a request to get product details"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
      - think: 1
