// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_get_success.csv"
    fields:
      - "productId"
    skipHeader: true
  ensure:
    - expect:
        - statusCode: 200
        - hasProperty: "name"
        - hasProperty: "description"
        - hasProperty: "price"
        - hasProperty: "id"
  plugins:
    expect: {}

scenarios:
  - name: "Find product by ID"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$.name"
              as: "name"
            - json: "$.description"
              as: "description"
            - json: "$.price"
              as: "price"
            - json: "$.id"
              as: "id"
          afterResponse: "logResponse"
      - think: 1

processors:
  logResponse:
    - log: "Received response for product with ID: {{ productId }}"
