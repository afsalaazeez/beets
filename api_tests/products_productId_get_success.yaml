# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_success.csv"
    fields:
      - "productId"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10

scenarios:
  - name: "Find product by ID"
    flow:
      - log: "Testing for successful operation"
      - get:
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 200 # Expecting successful operation
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
      - think: 1
