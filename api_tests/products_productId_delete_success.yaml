// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_success.csv"
    fields:
      - "productId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Delete product scenario"
    flow:
      - log: "Delete the product with given product id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "content-type"
        think: 2
