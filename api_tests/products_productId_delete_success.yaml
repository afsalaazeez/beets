# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework

# RoostTestHash=befb82c928


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "products_productId_delete_success.csv"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Delete product API test"
    flow:
      - log: "Delete product request"
        delete:
          url: "/products/{{ productId }}"
          headers:
            # Assuming 'name' field from SECURITY_SCHEMA is 'Authorization'
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            # Add more assertions as per API SPECIFICATION
        think: 5

      - log: "Another request"
        # Add more requests as per API SPECIFICATION
        # ...
