# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_success.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Delete product scenario"
    flow:
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest:
            log: "Sending request to delete product with ID: {{ productId }}"
          expect:
            - statusCode: 200
            - hasHeader: "content-type"
            - equals:
                message: "Product deleted"
          think: 1
