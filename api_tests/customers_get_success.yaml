# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /rest/customers_get for http method type GET in artillery framework

# RoostTestHash=c190dc1b92


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "customers_get_success.csv"
    fields:
      - "id"
      - "name"
      - "email"
    skipHeader: true

scenarios:
  - name: "Customer API tests"
    flow:
      - log: "Starting Customer API tests"
      - get:
          url: "/rest/customers"
          headers:
            {{ SECURITY_SCHEMA.name }}: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "email"
      - think: 1
      - log: "Finished Customer API tests"
