# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /rest/customers_get for http method type GET in artillery framework

# RoostTestHash=c190dc1b92


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "customers_get_success.csv"
    fields:
      - "id"
      - "name"
    skipHeader: true
  headers:
    "{{ SECURITY_SCHEMA.name }}": "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "API behavior with valid input payloads"
    flow:
      - get:
          url: "/rest/customers"
          headers:
            "{{ SECURITY_SCHEMA.name }}": "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "customers"
          # The API should return a 200 status code and a list of customers
        think: 1
      - log: "Get a list of customers - successful"
      - post:
          url: "/rest/customers"
          headers:
            "{{ SECURITY_SCHEMA.name }}": "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
           id: "{{ id }}"
           name: "{{ name }}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "name"
          # The API should return a 201 status code and the created customer
        think: 1
      - log: "Create a new customer - successful"
      - get:
          url: "/rest/customers/{{ id }}"
          headers:
            "{{ SECURITY_SCHEMA.name }}": "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
          # The API should return a 200 status code and the customer details
        think: 1
      - log: "Get customer details - successful"
      - delete:
          url: "/rest/customers/{{ id }}"
          headers:
            "{{ SECURITY_SCHEMA.name }}": "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 204
          # The API should return a 204 status code
        think: 1
      - log: "Delete a customer - successful"
