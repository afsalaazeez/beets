# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Testing failure and edge cases for /products API"
    flow:
      - log: "Sending a GET request with an invalid URL"
      - get:
          url: "/invalid-url"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
          - hasProperty: "error"
      - log: "The API should return a 404 status code with an error message"

      - think: 1

      - log: "Sending a GET request without authorization header"
      - get:
          url: "/products"
      - expect:
          - statusCode: 401
          - hasProperty: "error"
      - log: "The API should return a 401 status code indicating unauthorized access"

      - think: 1

      - log: "Sending a GET request with invalid authorization token"
      - get:
          url: "/products"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 401
          - hasProperty: "error"
      - log: "The API should return a 401 status code indicating invalid authorization token"

      - think: 1

      - log: "Sending a GET request with an invalid product id in the query parameter"
      - get:
          url: "/products?id=invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
      - log: "The API should return a 400 status code indicating bad request"

      - think: 1

      - log: "Sending a GET request with a non-existent product id in the query parameter"
      - get:
          url: "/products?id=9999999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
          - hasProperty: "error"
      - log: "The API should return a 404 status code indicating that the product does not exist"
