// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields: ["id", "name", "description", "price"]
    skipHeader: true
scenarios:
  - name: "Get all products failure cases"
    flow:
      - log: "Scenario 1: Sending a GET request without authorization"
        get:
          url: "/products"
          headers:
            Authorization: "Bearer invalid_token"
        expect:
          - statusCode: 401  # Unauthorized
          - hasProperty: "error"
          - equals: ["message", "Unauthorized"]
      - think: 1

      - log: "Scenario 2: Sending a GET request with an invalid URL"
        get:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404  # Not Found
          - hasProperty: "error"
          - equals: ["message", "Not Found"]
      - think: 1

      - log: "Scenario 3: Sending a GET request with an unsupported media type"
        get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "text/plain"
        expect:
          - statusCode: 415  # Unsupported Media Type
          - hasProperty: "error"
          - equals: ["message", "Unsupported Media Type"]
      - think: 1

      - log: "Scenario 4: Sending a GET request with an invalid Accept header"
        get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Accept: "text/html"
        expect:
          - statusCode: 406  # Not Acceptable
          - hasProperty: "error"
          - equals: ["message", "Not Acceptable"]
      - think: 1
