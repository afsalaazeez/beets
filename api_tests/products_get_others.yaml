// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields: ["id", "name", "description", "price"]
    skipHeader: true

scenarios:
  - name: "Product API tests"
    flow:
      - log: "Sending request with invalid URL"
        get:
          url: "/productss" # Invalid URL
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expected status code for invalid URL
            - hasProperty: "error"
        think: 1

      - log: "Sending request with invalid authorization"
        get:
          url: "/products"
          headers:
            Authorization: "InvalidToken" # Invalid authorization
          expect:
            - statusCode: 401 # Expected status code for invalid authorization
            - hasProperty: "error"
        think: 1

      - log: "Sending request with invalid header"
        get:
          url: "/products"
          headers:
            InvalidHeader: "InvalidValue" # Invalid header
          expect:
            - statusCode: 400 # Expected status code for invalid header
            - hasProperty: "error"
        think: 1

      - log: "Sending request with unexpected query parameters"
        get:
          url: "/products?unexpectedParam=value" # Unexpected query parameter
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # Expected status code for unexpected query parameter
            - hasProperty: "error"
        think: 1
