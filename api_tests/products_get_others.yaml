# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "products_get_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Products API failure cases"
    flow:
      - log: "Sending request with invalid URL"
        get:
          url: "/productss" # Invalid URL
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404 # As the URL is invalid, server should return 404
          - hasProperty: "message" # Assuming that server returns a message property in the response
        think: 1

      - log: "Sending request without authorization header"
        get:
          url: "/products"
        expect:
          - statusCode: 401 # Unauthorized request, server should return 401
          - hasProperty: "message" # Assuming that server returns a message property in the response
        think: 1

      - log: "Sending request with invalid authorization token"
        get:
          url: "/products"
          headers:
            Authorization: "InvalidToken" # Invalid Authorization token
        expect:
          - statusCode: 401 # Unauthorized request, server should return 401
          - hasProperty: "message" # Assuming that server returns a message property in the response
        think: 1

      - log: "Sending request with non-existing product id"
        get:
          url: "/products/9999999999" # Non-existing product id
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404 # Non-existing resource, server should return 404
          - hasProperty: "message" # Assuming that server returns a message property in the response
