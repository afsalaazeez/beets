# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields: ["id", "name", "description", "price"]
    skipHeader: true

scenarios:
  - name: "Get All Products - Failure and Edge Cases"
    flow:
      - think: 1
      - log: "Test Case: Invalid URL"
        get:
          url: "/productss" # Invalid URL
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}" # Authorization
          expect:
            - statusCode: 404 # Expected status code
        # The server should return a 404 status code as the URL is invalid.
      - think: 1
      - log: "Test Case: Unauthorized request"
        get:
          url: "/products" 
          headers:
            Authorization: "InvalidToken" # Invalid Authorization Token
          expect:
            - statusCode: 401 # Expected status code
        # The server should return a 401 status code as the authorization token is invalid.
      - think: 1
      - log: "Test Case: Invalid Header"
        get:
          url: "/products" 
          headers:
            InvalidHeader: "InvalidValue" # Invalid Header
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}" # Authorization
          expect:
            - statusCode: 400 # Expected status code
        # The server should return a 400 status code as the header is invalid.
