# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - flow:
      - log: "Invalid URL test case"
      - get:
          url: "/invalid_products"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        log: "Expected a 404 status code as the URL does not exist"
        think: 1
      - log: "Authorization failure test case"
      - get:
          url: "/products"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
        log: "Expected a 401 status code as the Authorization token is invalid"
        think: 1
      - log: "Invalid content-type in header test case"
      - get:
          url: "/products"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/xml"
          expect:
            - statusCode: 415
        log: "Expected a 415 status code as the content-type in header is invalid"
        think: 1
      - log: "Sending POST request instead of GET"
      - post:
          url: "/products"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
        log: "Expected a 405 status code as the method is not allowed"
        think: 1
