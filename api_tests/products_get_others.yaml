# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_get_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true
scenarios:
  - name: "Test invalid and edge cases for /products API"
    flow:
      - log: "Sending request with invalid URL"
        get:
          url: "/invalid_products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting 404 as the URL is invalid
        think: 1
      - log: "Sending request without authorization token"
        get:
          url: "/products"
          expect:
            - statusCode: 401 # Expecting 401 as the request is unauthorized
        think: 1
      - log: "Sending request with invalid authorization token"
        get:
          url: "/products"
          headers:
            Authorization: "Invalid token"
          expect:
            - statusCode: 401 # Expecting 401 as the token is invalid
        think: 1
      - log: "Sending request with valid URL but expecting 500 response"
        get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500 # Expecting 500 as this is an edge case
            - hasProperty: "error"
        think: 1
