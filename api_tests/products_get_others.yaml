# Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my-processor.js"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields: ["name", "description", "price"]
    skipHeader: true

scenarios:
  - name: "Get All Products Scenario"
    flow:
      - log: "Invalid Request: Invalid URL"
        get:
          url: "/prodcts"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
          - hasProperty: "error"
        # Expect a 404 Not Found error because the URL is invalid.

      - think: 5

      - log: "Invalid Request: Invalid Authorization Token"
        get:
          url: "/products"
          headers:
            Authorization: "Bearer invalid_token"
        expect:
          - statusCode: 403
          - hasProperty: "error"
        # Expect a 403 Forbidden error because the authorization token is invalid.

      - think: 5

      - log: "Invalid Request: Missing Authorization Header"
        get:
          url: "/products"
        expect:
          - statusCode: 401
          - hasProperty: "error"
        # Expect a 401 Unauthorized error because the Authorization header is missing.
