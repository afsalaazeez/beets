# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Test failure and edge cases for /products API"
    flow:
      - log: "Sending request with invalid URL"
        get:
          url: "/producs"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 1
      - log: "Sending request with invalid auth token"
        get:
          url: "/products"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        think: 1
      - log: "Sending request with extra query parameter"
        get:
          url: "/products?extra_param=true"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 1
      - log: "Sending request with missing required field in payload"
        post:
          url: "/products"
          json:
            name: "{{ name }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 1
      - log: "Sending request with invalid field type in payload"
        post:
          url: "/products"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "invalid_price"
          expect:
            - statusCode: 400
            - hasProperty: "error"
