# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Test Failure Cases for Get All Products API"
    flow:
      - log: "Sending request with missing Authorization token"
        get:
          url: "/products"
          headers:
            Authorization: ""
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - equals: 
              - "{{ response.error }}"
              - "Unauthorized"
      - think: 1
      - log: "Sending request with invalid Authorization token"
        get:
          url: "/products"
          headers:
            Authorization: "InvalidToken"
        expect:
          - statusCode: 403
          - hasProperty: "error"
          - equals: 
              - "{{ response.error }}"
              - "Forbidden"
      - think: 1
      - log: "Sending request to invalid URL"
        get:
          url: "/products_invalid"
        expect:
          - statusCode: 404
          - hasProperty: "error"
          - equals: 
              - "{{ response.error }}"
              - "Not Found"
      - think: 1
      - log: "Sending request with extra parameters"
        get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            extraParam: "extraValue"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - equals: 
              - "{{ response.error }}"
              - "Bad Request"
