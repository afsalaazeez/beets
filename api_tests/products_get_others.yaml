# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      ["name", "description", "price"]
    skipHeader: true

scenarios:
  - name: "Test Scenario"
    flow:
      - log: "Testing with invalid URL"
        get:
          url: "/invalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 2
      - log: "Testing without Authorization token"
        get:
          url: "/products"
          headers:
            Authorization: "Invalid Token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        think: 2
      - log: "Testing with invalid payload"
        get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "Invalid Name"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 2
      - log: "Testing with missing payload"
        get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            description: "Missing Name"
          expect:
            - statusCode: 400
            - hasProperty: "error"
