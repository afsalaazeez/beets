# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
scenarios:
  - name: "Get All Products - Failure Cases"
    flow:
      - log: "Test Case 1: Sending GET request with invalid URL"
        get:
          url: "/produc"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
          - hasProperty: "error"
      - think: 1
      - log: "Test Case 2: Sending GET request without Authorization header"
        get:
          url: "/products"
        expect:
          - statusCode: 401
          - hasProperty: "error"
      - think: 1
      - log: "Test Case 3: Sending GET request with invalid Authorization token"
        get:
          url: "/products"
          headers:
            Authorization: "InvalidToken"
        expect:
          - statusCode: 403
          - hasProperty: "error"
