# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# RoostTestHash=77bd829686

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_get_others.csv"
    fields:
      - "productId"
      - "userId"
  plugins:
    expect: {}
  environments:
    development:
      target: "http://dev.api.host"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
    production:
      target: "http://prod.api.host"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-success API responses test"
    flow:
      - log: "Sending an invalid GET request"
        get:
          url: "/rest/invalidProductPath/{{ productId }}"
          expect:
            - statusCode: 404
          think: 2

      - log: "Sending a GET request without authorization"
        get:
          url: "/rest/products/{{ productId }}"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
          think: 2

      - log: "Sending a GET request with invalid product ID"
        get:
          url: "/rest/products/invalidProductId"
          expect:
            - statusCode: 400
          think: 2
