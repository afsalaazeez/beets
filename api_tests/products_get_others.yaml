# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Get All Products failure and edge cases"
    flow:
      - log: "Sending request without authorization"
        get:
          url: "/products"
          headers:
            Authorization: "Invalid Auth"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        think: 5
      - log: "Sending request with invalid URL"
        get:
          url: "/invalid_products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 5
      - log: "Sending request with invalid product id in the URL"
        get:
          url: "/products/invalid_id"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 5
