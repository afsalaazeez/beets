# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Negative Testing for Product API"
    flow:
      - log: "Sending Invalid request with non-existing productId"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals:
                - "message"
                - "Product not found"
        think: 1
      - log: "Sending Invalid request with invalid URL"
        get:
          url: "/invalid/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Sending request with no Authorization token"
        get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 403
        think: 1
      - log: "Sending request with Invalid Authorization token"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 403
        think: 1
