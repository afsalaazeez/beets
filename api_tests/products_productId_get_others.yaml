# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Product failure and edge case scenarios"
    flow:
      - log: "Invalid product ID"
        get:
          url: "/products/abc"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasHeader: "content-type"
        think: 1
      - log: "Product ID not found"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasHeader: "content-type"
        think: 1
      - log: "Invalid authorization token"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasHeader: "content-type"
        think: 1
      - log: "Invalid URL"
        get:
          url: "/invalid_url/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasHeader: "content-type"
        think: 1
