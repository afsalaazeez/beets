# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Product API failure tests"
    flow:
      - log: "Executing test with invalid product ID"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
                - "message"
                - "Product not found"
      - think: 1
      - log: "Executing test with invalid authorization token"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: 
                - "error"
                - "Unauthorized"
      - think: 1
      - log: "Executing test with invalid URL"
        get:
          url: "/invalidURL/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals: 
                - "error"
                - "Not Found"
