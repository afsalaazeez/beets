# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test product API with invalid payloads and failure cases"
    flow:
      - log: "Sending request with invalid product ID"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasHeader: 'content-type'
        think: 1
      - log: "Sending request with invalid URL"
        get:
          url: "/invalid-url/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasHeader: 'content-type'
        think: 1
