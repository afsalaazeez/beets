// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Product API Tests"
    flow:
      - think: 5
      - log: "Sending request with valid product id but without authorization"
        get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - hasProperty: "error"
        # This request should fail with 401 Unauthorized status code as the request is not authenticated
      - think: 5
      - log: "Sending request with valid product id but with invalid authorization"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - hasProperty: "error"
        # This request should fail with 401 Unauthorized status code as the request is authenticated with invalid token
      - think: 5
      - log: "Sending request with invalid product id"
        get:
          url: "/products/invalidProductId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - hasProperty: "error"
        # This request should fail with 404 Not Found status code as the provided product id is invalid
      - think: 5
      - log: "Sending request to invalid URL"
        get:
          url: "/invalidURL/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        # This request should fail with 404 Not Found status code as the URL is invalid
