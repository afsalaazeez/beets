# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Test scenario for /products/{productId}"
    flow:
      - think: 5
      - log: "Test request with invalid product ID"
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                description: "Product not found"
      - log: "Test request with invalid URL"
      - get:
          url: "/invalidURL/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - log: "Test request with missing Authorization header"
      - get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401
