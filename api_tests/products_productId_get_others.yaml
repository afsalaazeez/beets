// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Product ID API negative test cases"
    flow:
      - log: "Invalid product id test"
        get:
          url: "/products/{{ productId }}invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
        think: 1

      - log: "Invalid URL test"
        get:
          url: "/productsinvalid/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Invalid authorization token test"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
        think: 1

      - log: "No authorization token test"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
        think: 1
