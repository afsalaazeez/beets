# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}  
  payload:
    path: "products_productId_get_others.csv"
    fields: 
      - "productId"
    skipHeader: true

scenarios:
  - name: "Non-success API response Test Scenario"
    flow:
      - log: "Sending request with invalid productId"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "errors"
        think: 1

      - log: "Sending request without authorization header"
        get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401
            - hasProperty: "errors"
        think: 1

      - log: "Sending request to non-existing endpoint"
        get:
          url: "/non-existing-endpoint"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Sending request with expired token"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer expired_token"
          expect:
            - statusCode: 401
            - hasProperty: "errors"
        think: 1
