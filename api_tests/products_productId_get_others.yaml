# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Product API testing for failure and edge cases"
    flow:
      - log: "Test with invalid product ID"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals: 
                - "{{ responseMessage }}": "Product not found"
        think: 1
      - log: "Test with invalid authorization token"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: 
                - "{{ responseMessage }}": "Unauthorized"
        think: 1
      - log: "Test with invalid URL"
        get:
          url: "/invalidURL/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
