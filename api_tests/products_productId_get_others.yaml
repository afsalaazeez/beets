# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    - think: 1
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields: ["productId"]
    skipHeader: true

scenarios:
  - name: "Scenario for testing failure and edge cases"
    flow:
      - log: "Testing with invalid product id"
        get:
          url: "/products/{{ productId }}"
          headers:
            authorization: "Bearer {{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 401 # Expecting unauthorized status code as invalid token is passed
            - hasProperty: "message"
            - equals: 
              - "{{ message }}"
              - "Invalid token"
      - think: 1
      - log: "Testing with non existent product id"
        get:
          url: "/products/9999999999" # Non existent product id
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting not found status code as non existent product id is passed
            - hasProperty: "message"
            - equals: 
              - "{{ message }}"
              - "Product not found"
      - think: 1
      - log: "Testing with invalid url"
        get:
          url: "/productss/{{ productId }}" # Invalid url
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting not found status code as invalid url is passed
            - hasProperty: "message"
            - equals: 
              - "{{ message }}"
              - "Invalid url"
