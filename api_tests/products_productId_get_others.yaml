# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true
  http:
    pool: 10
scenarios:
  - name: "Invalid product ID tests"
    flow:
      - log: "Invalid product ID test - Non-integer product ID"
        get:
          url: "/products/abc"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                - "description"
                - "Product not found"
      - think: 5
      - log: "Invalid product ID test - Negative product ID"
        get:
          url: "/products/-4"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                - "description"
                - "Product not found"
      - think: 5
      - log: "Invalid product ID test - Product ID not found"
        get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                - "description"
                - "Product not found"
