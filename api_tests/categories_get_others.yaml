# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# RoostTestHash=9c39e39ed8

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 5
      arrivalRate: 1
  payload:
    path: "categories_get_others.csv"
    fields:
      - "categoryId"
      - "categoryName"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      - log: "Sending a GET request with invalid URL"
        get:
          url: "/rest/invalidURL"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404
        think: 1

      - log: "Sending a GET request without authorization token"
        get:
          url: "/rest/categories"
        expect:
          - statusCode: 401
        think: 1

      - log: "Sending a GET request with invalid authorization token"
        get:
          url: "/rest/categories"
          headers:
            Authorization: "Bearer invalid_token"
        expect:
          - statusCode: 403
        think: 1

      - log: "Sending a GET request with invalid category ID"
        get:
          url: "/rest/categories/{{ categoryId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 400
        think: 1

      - log: "Sending a GET request expecting server error"
        get:
          url: "/rest/categories/{{ categoryName }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 500
        think: 1
