# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# Test generated for /products_post for http method type POST in artillery framework
# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./products_post_success.csv"
    fields: ["id", "name", "description", "price"]
    skipHeader: true

scenarios:
  - name: "Add a new product to the store"
    flow:
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
          log: "Post request to add a new product to the store"
      - think: 1
