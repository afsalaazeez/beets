# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /rest/categories_get for http method type GET in artillery framework

# RoostTestHash=9c39e39ed8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "categories_get_success.csv"
    fields: 
      - "field1"
      - "field2"
    skipHeader: true
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Test Scenario"
    flow:
      - get:
          url: "/rest/categories"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "field1"
            - hasProperty: "field2"
          afterResponse: "log"
      - think: 1
