# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /rest/categories_get for http method type GET in artillery framework

# RoostTestHash=9c39e39ed8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "categories_get_success.csv"
    fields:
      - "categoryName"
      - "categoryId"
    skipHeader: true
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "API Test Scenario"
    flow:
      - get:
          url: "/rest/categories"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "categories"
          capture:
            - json: "$.categories[0].id"
              as: "firstCategoryId"
          log: "Retrieved list of categories successfully"
        think: 1

      - get:
          url: "/rest/categories/{{ firstCategoryId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "category"
          log: "Retrieved a single category successfully"
        think: 1
