# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# RoostTestHash=0377fbf6fd

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 5
      arrivalRate: 1
  payload:
    path: "countries_get_others.csv"
    fields:
      - "CountryName"
      - "CountryCode"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending a GET request with invalid URL"
        get:
          url: "/rest/countries/invalidURL"
          headers:
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404
        think: 1

      - log: "Sending a GET request without authorization token"
        get:
          url: "/rest/countries/{{ CountryCode }}"
        expect:
          - statusCode: 401
        think: 1

      - log: "Sending a GET request with invalid authorization token"
        get:
          url: "/rest/countries/{{ CountryCode }}"
          headers:
            authorization: "Bearer invalidToken"
        expect:
          - statusCode: 403
        think: 1

      - log: "Sending a GET request with non-existing country code"
        get:
          url: "/rest/countries/nonExistingCountryCode"
          headers:
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404
        think: 1

      - log: "Sending a GET request expecting server error"
        get:
          url: "/rest/countries/{{ CountryCode }}"
          headers:
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 500
        think: 1
