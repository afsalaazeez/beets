# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# RoostTestHash=1f6a496b8b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 5
      arrivalRate: 1
  payload:
    path: "items_get_others.csv"
    fields:
      - "itemID"
  plugins:
    expect: {}

scenarios:
  - name: "Testing Non-Success API responses"
    flow:
      - log: "Testing with incorrect API endpoint"
        get:
          url: "/invalid_endpoint"
          expect:
            - statusCode: 404
        think: 1

      - log: "Testing with invalid payload"
        post:
          url: "/rest/items"
          json:
            itemID: "invalid_item_id"
          expect:
            - statusCode: 400
        think: 1

      - log: "Testing without required authentication token"
        headers:
          Authorization: "InvalidToken"
        get:
          url: "/rest/items"
          expect:
            - statusCode: 403
        think: 1

      - log: "Testing with correct API endpoint but expecting Internal Server Error"
        get:
          url: "/rest/items"
          expect:
            - statusCode: 500
        think: 1
