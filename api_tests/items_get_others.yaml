# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# RoostTestHash=1f6a496b8b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "items_get_others.csv"
    fields:
      - "itemId"
  plugins:
    expect: {}
  processor: "./my-functions.js"
  
scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending a GET request with invalid URL"
        get:
          url: "/invalid/url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404  # Expecting a 404 Not Found response
        think: 1

      - log: "Sending a GET request without authorization"
        get:
          url: "/rest/items"
        expect:
          - statusCode: 401  # Expecting a 401 Unauthorized response
        think: 1

      - log: "Sending a GET request with incorrect authorization"
        get:
          url: "/rest/items"
          headers:
            Authorization: "Bearer incorrect_token"
        expect:
          - statusCode: 403  # Expecting a 403 Forbidden response
        think: 1

      - log: "Sending a GET request with valid authorization but non-existent item"
        get:
          url: "/rest/items/{{ itemId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404  # Expecting a 404 Not Found response
        think: 1

      - log: "Triggering a 500 Internal Server Error"
        get:
          url: "/rest/items"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          beforeRequest:
            - "triggerInternalServerError"  # Custom function to trigger a 500 error
        expect:
          - statusCode: 500  # Expecting a 500 Internal Server Error response
        think: 1
