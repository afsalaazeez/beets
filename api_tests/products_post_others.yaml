// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_post_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test failure cases for POST /products"
    flow:
      - think: 2
      - post:
          url: "/products"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          log: "Sending a valid request with valid payload"
          expect:
            - statusCode: 405
            - hasProperty: "message"
            - equals: 
              - "message"
              - "Invalid input"
      - think: 2
      - post:
          url: "/products"
          headers:
            Authorization: "InvalidToken"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          log: "Sending a valid request with invalid authorization"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
              - "message"
              - "Unauthorized"
      - think: 2
      - post:
          url: "/invalidEndpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          log: "Sending a valid request to invalid endpoint"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
              - "message"
              - "Not found"
