# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_post_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Product API failure cases"
    flow:
      - log: "Sending invalid request with missing required field 'name'"
        post:
          url: "/products"
          json:
            id: "{{ id }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
            - equals: 
                - "description"
                - "Invalid input"
        think: 1
      - log: "Sending request to invalid URL"
        post:
          url: "/invalidURL"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Sending request with invalid authorization token"
        post:
          url: "/products"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
        think: 1
