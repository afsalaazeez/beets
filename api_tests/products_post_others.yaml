# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_post_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Product creation failure cases"
    flow:
      - log: "Sending a request with missing required field"
        post:
          url: "/products"
          json:
            id: "{{ id }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
            - equals:
                - "{{ description }}"
                - "Invalid input"
        think: 2
      - log: "Sending a request with invalid URL"
        post:
          url: "/invalid_url"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 2
      - log: "Sending a request with invalid authorization"
        post:
          url: "/products"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 401
        think: 2
      - log: "Sending a request with incorrect payload format"
        post:
          url: "/products"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "invalid price"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 2
