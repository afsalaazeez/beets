// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_post_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
scenarios:
  - name: "Create Product scenario"
    flow:
      - post:
          url: "/products"
          headers:
            content-type: "application/json"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
          log: "Attempt to create a product with invalid data"
      - think: 5
      - post:
          url: "/products_invalid_url"
          headers:
            content-type: "application/json"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 404
          log: "Attempt to create a product with invalid URL"
      - think: 5
      - post:
          url: "/products"
          headers:
            content-type: "application/json"
            authorization: "Invalid auth token"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 401
          log: "Attempt to create a product with invalid authorization token"
