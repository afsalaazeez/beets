// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=7b2a6b6959

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_put_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Update product API failure scenarios"
    flow:
      - log: "Testing with invalid product ID"
        put:
          url: "/products/invalidID"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 1

      - log: "Testing with missing name in request body"
        put:
          url: "/products/{{ id }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1

      - log: "Testing with invalid authorization token"
        put:
          url: "/products/{{ id }}"
          headers:
            Content-Type: "application/json"
            Authorization: "InvalidToken"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1

      - log: "Testing with invalid request body"
        put:
          url: "/products/{{ id }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "invalidPrice"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
