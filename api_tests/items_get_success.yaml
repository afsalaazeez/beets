# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /rest/items_get for http method type GET in artillery framework

# RoostTestHash=1f6a496b8b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "items_get_success.csv"
    fields:
      - "item_id"
    skipHeader: true
  headers:
    "Authorization": "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "API Test Scenario"
    flow:
      - get:
          url: "/rest/items/{{ item_id }}"
          headers:
            "Content-Type": "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "item_id"
          afterResponse: "logResponse"
      - think: 1

processors:
  logResponse:
    - function: "log"
      options:
        message: "Response: {{ response }}"
