# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# Test generated for /rest/items_get for http method type GET in artillery framework

# RoostTestHash=1f6a496b8b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "items_get_success.csv"
    fields: 
      - "item_id"
      - "item_name"
    skipHeader: true
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
  processor: "./my-processor.js"

scenarios:
  - name: "Get a list of items"
    flow:
      - get:
          url: "/rest/items"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "items"
            - hasProperty: "items.item_id"
            - hasProperty: "items.item_name"
      - log: "Received a list of items"
      - think: 1
