# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Delete product API failure cases"
    flow:
      - log: "Sending request with a productId that does not exist"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 1
      - log: "Sending request with an invalid URL"
        delete:
          url: "/invalidURL/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 1
      - log: "Sending request with incorrect authorization token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Invalid_Token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        think: 1
