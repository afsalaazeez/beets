# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
scenarios:
  - name: "Delete Product API - Failure and Edge Cases"
    flow:
      - log: "Invalid URL test"
        delete:
          url: "/produts/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 2
      - log: "Invalid authorization token test"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 403
            - hasProperty: "message"
      - think: 2
      - log: "Non-existent product ID test"
        delete:
          url: "/products/9999999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
