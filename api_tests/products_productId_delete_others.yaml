# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Delete product - Negative test cases"
    flow:
      - log: "Sending a delete request to delete a product with invalid ID"
        delete:
          url: "/products/{{ productId }}"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
              - "message": "Product not found"
        think: 1

      - log: "Sending a delete request to delete a product with no authorization token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            authorization: "InvalidAuthToken"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
              - "message": "Unauthorized"
        think: 1

      - log: "Sending a delete request to delete a product with invalid url"
        delete:
          url: "/invalidUrl/{{ productId }}"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
              - "message": "Not Found"
        think: 1
