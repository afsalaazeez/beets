# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
scenarios:
  - name: "Delete product API failure scenarios"
    flow:
      - think: 1
      - log: "Sending invalid product id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
              - "{{ response.body.message }}"
              - "Product not found"
      - think: 1
      - log: "Sending request to invalid URL"
        delete:
          url: "/invalidURL/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
              - "{{ response.body.message }}"
              - "Not found"
      - think: 1
      - log: "Sending request with invalid authorization token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "invalidAuthToken"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
              - "{{ response.body.message }}"
              - "Unauthorized"
