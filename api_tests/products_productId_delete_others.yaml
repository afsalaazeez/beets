# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4


# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields: ["productId"]
    skipHeader: true

scenarios:
  - name: "Test non-success API responses"
    flow:
      - delete:
           url: "/products/{{ productId }}"
           headers:
             Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
           expect:
             - statusCode: 404
             - hasProperty: "errors"
           # Expecting a 404 Not Found response as the product does not exist
           capture:
             json: "$.errors[0]"
             as: "errorMessage"
           think: 1

      - log: "The error message for the delete request was {{ errorMessage }}"

      - get:
           url: "/invalidURL"
           headers:
             Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
           expect:
             - statusCode: 404
           # Expecting a 404 Not Found response as the URL is invalid
           capture:
             json: "$.errors[0]"
             as: "errorMessage"
           think: 1

      - log: "The error message for the get request was {{ errorMessage }}"
