# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Delete product failure scenarios"
    flow:
      - log: "Sending request with non-existing product id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals: 
              - "{{ responseMessage }}"
              - "Product not found"
        think: 1
      - log: "Sending request without product id"
        delete:
          url: "/products/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals:
              - "{{ responseMessage }}"
              - "Bad request"
        think: 1
      - log: "Sending request with invalid url"
        delete:
          url: "/invalidurl/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals:
              - "{{ responseMessage }}"
              - "Not found"
        think: 1
      - log: "Sending request with invalid authorization token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals:
              - "{{ responseMessage }}"
              - "Unauthorized"
        think: 1
