// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields: ["productId"]
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Delete product negative tests"
    flow:
      - log: "Sending request with invalid product id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401 # Expecting unauthorized due to invalid token
            - hasProperty: "message"
      - think: 1
      - log: "Sending request with non-existing product id"
        delete:
          url: "/products/99999"
          headers:
            Authorization: "Bearer {{ $environment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting not found due to non-existing product
            - hasProperty: "message"
      - think: 1
      - log: "Sending request to invalid URL"
        delete:
          url: "/invalid_url/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $environment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting not found due to invalid URL
            - hasProperty: "message"
