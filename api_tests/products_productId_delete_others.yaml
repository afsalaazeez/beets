// Test generated by RoostGPT for test beetbox-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Delete product API failure test cases"
    flow:
      - log: "Test Case: Trying to delete a product with invalid product Id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 1
      - log: "Test Case: Trying to delete a product with no product Id in path"
        delete:
          url: "/products/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 1
      - log: "Test Case: Trying to delete a product with invalid url"
        delete:
          url: "/invalidUrl/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 1
      - log: "Test Case: Trying to delete a product with invalid authorization token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Invalid token"
          expect:
            - statusCode: 401
            - hasProperty: "message"
