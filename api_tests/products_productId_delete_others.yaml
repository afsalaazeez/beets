# Test generated by RoostGPT for test beet-box using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Product deletion failure scenarios"
    flow:
      - log: "Sending DELETE request with invalid product id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 5
      - log: "Sending DELETE request with invalid URL"
        delete:
          url: "/invalid_url/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 5
      - log: "Sending DELETE request with invalid Authorization token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        think: 5
