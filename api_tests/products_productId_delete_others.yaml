# Test generated by RoostGPT for test beetbox using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Delete product scenarios"
    flow:
      - log: "Scenario: Trying to delete a product with invalid productId"
        delete:
          url: "/products/invalidProductId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Scenario: Trying to delete a product with unauthorized token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Invalid token"
          expect:
            - statusCode: 403
      - think: 1
      - log: "Scenario: Trying to delete a product with invalid url"
        delete:
          url: "/invalidUrl/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Scenario: Trying to delete a product without any authorization token"
        delete:
          url: "/products/{{ productId }}"
          headers: {}
          expect:
            - statusCode: 403
